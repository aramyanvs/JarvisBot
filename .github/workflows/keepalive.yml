name: keepalive
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health
        run: curl -fsS "$HEALTH_URL"
        env:
          HEALTH_URL: ${{ secrets.HEALTH_URL }}
